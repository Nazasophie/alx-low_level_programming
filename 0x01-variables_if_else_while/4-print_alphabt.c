#include <stdio.h>
/**
 * main - Ommit some alphabet
 *
 * Return: Always (success)
 */
int main(void)
{
	char low, e, q;

	e = 'e';
	q = 'q'
		for (low = 'a'; low <= 'z'; low++)
		{
			if (low != e && low != q)
				putchar(low);
			return (0);
